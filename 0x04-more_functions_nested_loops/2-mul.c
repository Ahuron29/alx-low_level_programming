#include "main.h"
/**
 * mul - multiplies two numbers returns the product
 * @a: is one of the numbers
 * @b: is other number.
 * Return: the product of a * b
 */
int mul(int a, int b)
{
return (a * b);
}
